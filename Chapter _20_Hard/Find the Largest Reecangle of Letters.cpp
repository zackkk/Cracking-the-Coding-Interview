/* Given a dictionary of millions of words, 
   give an algorithm to find the largest possible rectangle of letters 
   such that every row forms a word (reading left to right) and every column forms a word (reading top to bottom).*/

// group words based of their size
// start from the group with the biggest size